// Generating a random Rust code snippet
let mut rng = rand::thread_rng();

// Get a random number between 1 and 5
let num = rng.gen_range(1..=5);

// Print the number to the console
println!("Random number: {}", num);
